s1 = "waterbottle"
s2 = 'bottlewater'

def isRotation(s1,s2):
    if (len(s1)==len(s2) & len(s1) >0):
        s1s1 = s1 + s1
        if s2 in s1s1:
            return True
    return False

isRotation(s1,s2)
